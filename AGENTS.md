# Project Instructions

## Git Synchronization Policy
- Every change to HTML, CSS, or JS should be prepared for immediate synchronization to GitHub (`main` branch) for GitHub Pages hosting.
- The repository uses `autosync.sh` and `sync.sh` for seamless synchronization to GitHub Pages.
